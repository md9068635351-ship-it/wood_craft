import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { getAdminSession } from "@/lib/auth";
import { cleanString, databaseUnavailable, jsonError, optionalString, slugify } from "@/lib/api";

export async function GET(_request,{params}){if(!getAdminSession())return jsonError("Unauthorized",401);try{const product=await prisma.product.findUnique({where:{id:params.id},include:{images:{orderBy:{order:"asc"}},videos:{orderBy:{order:"asc"}},category:true}});return product?NextResponse.json({product}):jsonError("Product not found",404)}catch(error){return databaseUnavailable(error)}}
export async function PUT(request,{params}){if(!getAdminSession())return jsonError("Unauthorized",401);let body;try{body=await request.json()}catch{return jsonError("Invalid JSON body")};const name=cleanString(body.name,160),description=cleanString(body.description,5000),price=Number(body.price),stock=Number(body.stock);if(!name||!description||!body.categoryId||!Number.isFinite(price)||price<=0)return jsonError("Name, description, valid price, and category are required");if(!Number.isInteger(stock)||stock<0)return jsonError("Invalid stock");try{const product=await prisma.$transaction(async tx=>{await tx.productImage.deleteMany({where:{productId:params.id}});await tx.productVideo.deleteMany({where:{productId:params.id}});return tx.product.update({where:{id:params.id},data:{name,slug:slugify(body.slug||name),description,story:optionalString(body.story,5000),price,comparePrice:body.comparePrice?Number(body.comparePrice):null,woodType:optionalString(body.woodType,120),dimensions:optionalString(body.dimensions,200),weight:optionalString(body.weight,100),careInfo:optionalString(body.careInfo,2000),stock,isCustomizable:Boolean(body.isCustomizable),isFeatured:Boolean(body.isFeatured),isActive:body.isActive!==false,categoryId:body.categoryId,images:{create:(body.images||[]).map((image,index)=>({url:image.url,altText:image.altText||name,order:index}))},videos:{create:(body.videos||[]).map((video,index)=>({url:video.url,title:video.title||name,order:index}))}},include:{images:true,videos:true,category:true}})});return NextResponse.json({product})}catch(error){if(error?.code==="P2025")return jsonError("Product not found",404);if(error?.code==="P2002")return jsonError("A product with this slug already exists",409);return databaseUnavailable(error)}}
export async function DELETE(_request,{params}){if(!getAdminSession())return jsonError("Unauthorized",401);try{await prisma.product.delete({where:{id:params.id}});return NextResponse.json({success:true})}catch(error){if(error?.code==="P2003")return jsonError("Products used in existing orders cannot be deleted; hide it instead",409);if(error?.code==="P2025")return jsonError("Product not found",404);return databaseUnavailable(error)}}
