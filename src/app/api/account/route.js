import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { getCustomerSession } from "@/lib/auth";
import { cleanString, isPhone, jsonError, optionalString } from "@/lib/api";
export async function GET(){const session=getCustomerSession();if(!session)return jsonError("Authentication required",401);const user=await prisma.user.findUnique({where:{id:session.userId},select:{id:true,name:true,email:true,phone:true,address:true,city:true,pincode:true,createdAt:true,orders:{orderBy:{createdAt:"desc"},include:{items:{include:{product:{select:{name:true,slug:true,images:{take:1}}}}}}}}});return user?NextResponse.json({user}):jsonError("Account not found",404)}
export async function PUT(request){const session=getCustomerSession();if(!session)return jsonError("Authentication required",401);let body;try{body=await request.json()}catch{return jsonError("Invalid request")};const name=cleanString(body.name,100),phone=optionalString(body.phone,20);if(!name)return jsonError("Name is required");if(phone&&!isPhone(phone))return jsonError("Phone is invalid");const user=await prisma.user.update({where:{id:session.userId},data:{name,phone,address:optionalString(body.address,500),city:optionalString(body.city,100),pincode:optionalString(body.pincode,12)},select:{id:true,name:true,email:true,phone:true,address:true,city:true,pincode:true}});return NextResponse.json({user})}
