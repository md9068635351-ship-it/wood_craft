"use client";
import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Eye, EyeOff, Loader2, LockKeyhole } from "lucide-react";

export default function CustomerLoginPage(){const router=useRouter();const[mode,setMode]=useState('login'),[form,setForm]=useState({name:'',email:'',phone:'',password:''}),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e){e.preventDefault();setLoading(true);setError('');const response=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await response.json();setLoading(false);if(!response.ok)return setError(data.error||'Something went wrong');const next=new URLSearchParams(window.location.search).get('next')||'/account';router.replace(next);router.refresh()}const input="mt-1.5 w-full rounded-xl border border-walnut/15 bg-paper px-4 py-3 text-sm outline-none focus:border-rust";return <main className="wood-surface flex min-h-[75vh] items-center justify-center px-5 py-12"><div className="w-full max-w-md rounded-3xl border border-walnut/10 bg-paper/95 p-6 shadow-2xl shadow-walnut/10 sm:p-9"><div className="text-center"><Image src="/brand/infinity-creations-logo.jpeg" alt="Infinity Creations" width={82} height={82} className="mx-auto rounded-full border border-walnut/10 object-cover"/><h1 className="mt-4 font-display text-3xl font-semibold">{mode==='login'?'Welcome back':'Create your account'}</h1><p className="mt-1 text-sm text-ink/50">{mode==='login'?'Sign in to checkout and track orders.':'Save addresses and view every order in one place.'}</p></div><div className="mt-6 grid grid-cols-2 rounded-full bg-paper-warm p-1"><button onClick={()=>{setMode('login');setError('')}} className={`rounded-full py-2 text-sm font-semibold ${mode==='login'?'bg-walnut text-paper shadow':'text-ink/55'}`}>Sign in</button><button onClick={()=>{setMode('register');setError('')}} className={`rounded-full py-2 text-sm font-semibold ${mode==='register'?'bg-walnut text-paper shadow':'text-ink/55'}`}>Register</button></div><form onSubmit={submit} className="mt-6 space-y-4">{mode==='register'&&<><label className="block text-sm font-medium">Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className={input}/></label><label className="block text-sm font-medium">WhatsApp number<input required type="tel" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} className={input}/></label></>}<label className="block text-sm font-medium">Email address<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className={input}/></label><label className="block text-sm font-medium">Password<div className="relative"><input required minLength={8} type={show?'text':'password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className={`${input} pr-12`}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 mt-1 -translate-y-1/2 p-1 text-ink/40">{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<p className="rounded-xl bg-rust/10 p-3 text-sm text-rust">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-full bg-rust px-6 py-3.5 text-sm font-bold text-white disabled:opacity-60">{loading?<Loader2 className="animate-spin" size={17}/>:<LockKeyhole size={17}/>} {mode==='login'?'Sign in securely':'Create account'}</button></form><p className="mt-5 text-center text-xs text-ink/40">By continuing, you agree to our store policies.</p></div></main>}
